export class UnitModel {
  constructor(id, title, price, material, faction, inStock, img_url) {
    this.id = id;
    this.title = title;
    this.price = price;
    this.material = material;
    this.faction = faction;
    this.inStock = inStock;
    this.img_url = img_url;
  }
}
